/// Ilova versiyasi — [pubspec.yaml] `version:` bilan bir xil bo‘lishi kerak.
abstract final class AppVersion {
  static const name = '1.0.12';
  static const build = 28;
  static const label = '$name+$build';
}
